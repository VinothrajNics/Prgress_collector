{
  "name": "collect-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute collect-db --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute collect-db --remote --file=./schema.sql"
  },
  "dependencies": {
    "hono": "^4.13.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260827.1",
    "wrangler": "^4.127.0"
  }
}
